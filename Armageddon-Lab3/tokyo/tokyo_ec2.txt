resource "aws_instance" "tokyo_test" {
  provider               = aws.tokyo
  ami                    = data.aws_ami.amazon_linux.id
  instance_type          = "t3.micro"
  subnet_id              = aws_subnet.tokyo_private_subnet01.id
  vpc_security_group_ids = [aws_security_group.tgw_test_sg.id]

  tags = { Name = "tokyo-tgw-test" }
}
